import { createIconSetFromIcoMoon } from 'react-native-vector-icons'
import icoMoonConfig from '../icomoon_gen/selection.json'

const icon = createIconSetFromIcoMoon(icoMoonConfig)

export default icon
